{% extends 'base.html.twig' %}

{% block title %}Create New Agence{% endblock %}

{% block body %}
<h1>Create New Agence</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Save</button>
{{ form_end(form) }}
<a href="{{ path('agence_index') }}">Back to list</a>
{% endblock %}
